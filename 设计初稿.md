# 服务端登录插件
## 目标
构建一个服务端登录插件，部署在公网服务器上，让用户可以随时通过域名安全的访问DSH。
## 详细设计
### 登录
需要先创建管理员账号，其他用户注册时是需管理员审核。（为管理员账号创建单独的管理员界面）每个用户的文件环境配置密钥独立以及插件存放，仅自己可读。
### DSH环境
DSH环境中应当同时运行两个DSH,但共享同一个与用户对话，主DSH负责正常工作，另一个DSH负责保证主DSH的稳定运行，在主DSH崩溃是负责修理，以及主DSH进行需重启插件安装时的重启任务，主DSH需重启时要先给出重启后自动命令，有另一个DSH执行。
### 登录桌面
登录后用户进入一个简单桌面，在这展示用户所有文件以及DSH启动入口，并且用户也可以在文件夹直接以该文件夹启动（同时DSH要储存不同工程文件夹的插件以及其他配置），若用户在桌面上直接启动，需要先展示用户已有所有DSH插件并展示插件简单介绍，用户可以选择启用哪些插件。
### 访问
默认使用域名访问，预留域名配置与nginx配置接口，用户可以自定义域名访问DSH。
### 安全
用户不能通过DSH访问其他用户文件以及服务器文件，做好防注入措施。
### 插件撰写规范
插件需要遵循一定的撰写规范，以确保其安全性和稳定性。  
1、开饭api及相关规范 deepseek-harness\docs  
2、插件市场收录与安装规范 DSH-sever-login\STANDARD.md